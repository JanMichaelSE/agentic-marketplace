# Architecture Vocabulary Moved

`codebase-design` owns the definitions and guidance for deep modules,
interfaces, seams, leverage, and locality. Read its
[SKILL.md](../../../software-architecture/skills/codebase-design/SKILL.md) when that
optional companion plugin is installed.

TDD remains usable without that plugin: agree on the caller-facing test seam
and observable behavior without recreating competing architecture definitions.